{
  "name": "mrbeast",
  "version": "0.1.0",
  "private": true,
  "description": "MrZvir MVP demo",
  "scripts": {
    "test": "node --test tests/unit",
    "test:coverage": "node --test --coverage --coverage-report=html tests/unit",
    "test:broken": "BUG_MODE=1 node --test tests/unit",
    "lint": "eslint .",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "playwright": "^1.43.0"
  }
}
